{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public",
    "restartPolicyType": "ON_FAILURE"
  }
}